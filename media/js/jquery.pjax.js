(function($){$.fn.pjax=function(container,options){return this.live("click.pjax",function(event){handleClick(event,container,options)})};function handleClick(event,container,options){options=optionsFor(container,options);var link=event.currentTarget;if(link.tagName.toUpperCase()!=="A")throw"$.fn.pjax or $.pjax.click requires an anchor element";if(event.which>1||event.metaKey)return;if(location.protocol!==link.protocol||location.host!==link.host)return;if(link.hash&&link.href.replace(link.hash,"")===location.href.replace(location.hash,""))return;var defaults={url:link.href,container:$(link).attr("data-pjax"),target:link,clickedElement:$(link),fragment:null};$.pjax($.extend({},defaults,options));event.preventDefault()}var pjax=$.pjax=function(options){options=$.extend(true,{},$.ajaxSettings,pjax.defaults,options);if($.isFunction(options.url)){options.url=options.url()}var target=options.target;if(!target&&options.clickedElement)target=options.clickedElement[0];var hash=parseURL(options.url).hash;var oldBeforeSend=options.beforeSend,oldComplete=options.complete,oldSuccess=options.success,oldError=options.error;var context=options.context=findContainerFor(options.container);if(!options.data)options.data={};options.data._pjax=context.selector;function fire(type,args){var event=$.Event(type,{relatedTarget:target});context.trigger(event,args);return!event.isDefaultPrevented()}var timeoutTimer,durationTimer;var success,complete,error;options.beforeSend=function(xhr,settings){var timeout=settings.timeout;if(settings.timeout>0){timeoutTimer=setTimeout(function(){if(!fire("pjax:timeout",[xhr,options]))return;if(durationTimer)clearTimeout(durationTimer);xhr.abort("timeout")},settings.timeout);settings.timeout=0}xhr.setRequestHeader("X-PJAX","true");xhr.setRequestHeader("X-PJAX-Container",context.selector);var result;if(oldBeforeSend){result=oldBeforeSend.apply(this,arguments);if(result===false)return false}if(!fire("pjax:beforeSend",[xhr,settings]))return false;var duration=settings.duration;if(timeoutTimer&&duration>=timeout)duration=timeout-1;if(duration>=0&&duration!=null)durationTimer=setTimeout(function(){durationTimer=null;if(complete){if(success)success();if(error)error();complete();return}fire("pjax:waiting",[xhr,options])},duration);if(options.push&&!options.replace){containerCache.push(pjax.state.id,context.clone(true,true).contents());window.history.pushState(null,"",options.url)}fire("pjax:start",[xhr,options]);fire("start.pjax",[xhr,options]);fire("pjax:send",[xhr,settings])};options.complete=function(xhr,textStatus){complete=function(){_complete.call(options,xhr,textStatus)};if(!durationTimer)complete()};function _complete(xhr,textStatus){if(timeoutTimer)clearTimeout(timeoutTimer);if(oldComplete)oldComplete.apply(this,arguments);fire("pjax:complete",[xhr,textStatus,options]);fire("pjax:end",[xhr,options]);fire("end.pjax",[xhr,options])}options.error=function(xhr,textStatus,errorThrown){error=function(){_error.call(xhr,textStatus,errorThrown)};if(!durationTimer)error()};function _error(xhr,textStatus,errorThrown){var container=extractContainer("",xhr,options);if(oldError)oldError.apply(this,arguments);var allowed=fire("pjax:error",[xhr,textStatus,errorThrown,options]);if(textStatus!=="abort"&&allowed)window.location=container.url}options.success=function(data,status,xhr){success=function(){_success.call(options,data,status,xhr)};if(!durationTimer)success()};function _success(data,status,xhr){var container=extractContainer(data,xhr,options);if(!container.contents){window.location=container.url;return}pjax.state={id:options.id||uniqueId(),url:container.url,container:context.selector,fragment:options.fragment,timeout:options.timeout,duration:options.duration};if(options.push||options.replace){window.history.replaceState(pjax.state,container.title,container.url)}if(container.title)document.title=container.title;context.html(container.contents);if(typeof options.scrollTo==="number")$(window).scrollTop(options.scrollTo);if((options.replace||options.push)&&window._gaq)_gaq.push(["_trackPageview"]);if(hash!==""){window.location.href=hash}if(oldSuccess)oldSuccess.apply(this,arguments);fire("pjax:success",[data,status,xhr,options])}if(!pjax.state){pjax.state={id:uniqueId(),url:window.location.href,container:context.selector,fragment:options.fragment,timeout:options.timeout};window.history.replaceState(pjax.state,document.title)}var xhr=pjax.xhr;if(xhr&&xhr.readyState<4){xhr.onreadystatechange=$.noop;xhr.abort()}pjax.options=options;pjax.xhr=$.ajax(options);$(document).trigger("pjax",[pjax.xhr,options]);return pjax.xhr};function uniqueId(){return(new Date).getTime()}function stripPjaxParam(url){return url.replace(/\?_pjax=[^&]+&?/,"?").replace(/_pjax=[^&]+&?/,"").replace(/[\?&]$/,"")}function parseURL(url){var a=document.createElement("a");a.href=url;return a}function optionsFor(container,options){if(container&&options)options.container=container;else if($.isPlainObject(container))options=container;else options={container:container};if(options.container)options.container=findContainerFor(options.container);return options}function findContainerFor(container){container=$(container);if(!container.length){throw"no pjax container for "+container.selector}else if(container.selector!==""&&container.context===document){return container}else if(container.attr("id")){return $("#"+container.attr("id"))}else{throw"cant get selector for pjax container!"}}function findAll(elems,selector){var results=$();elems.each(function(){if($(this).is(selector))results=results.add(this);results=results.add(selector,this)});return results}function extractContainer(data,xhr,options){var obj={};obj.url=stripPjaxParam(xhr.getResponseHeader("X-PJAX-URL")||options.url);var $data=$(data);if($data.length===0)return obj;obj.title=findAll($data,"title").last().text();if(options.fragment){var $fragment=findAll($data,options.fragment).first();if($fragment.length){obj.contents=$fragment.contents();if(!obj.title)obj.title=$fragment.attr("title")||$fragment.data("title")}}else if(!/<html/i.test(data)){obj.contents=$data}if(obj.contents){obj.contents=obj.contents.not("title");obj.contents.find("title").remove()}if(obj.title)obj.title=$.trim(obj.title);return obj}pjax.reload=function(container,options){var defaults={url:window.location.href,push:false,replace:true,scrollTo:false};return $.pjax($.extend(defaults,optionsFor(container,options)))};pjax.defaults={timeout:5e3,push:true,replace:false,type:"GET",dataType:"html",scrollTo:0,maxCacheLength:20};function Cache(){this.mapping={};this.forwardStack=[];this.backStack=[]}Cache.prototype.push=function(id,value){this.mapping[id]=value;this.backStack.push(id);while(this.forwardStack.length)delete this.mapping[this.forwardStack.shift()];while(this.backStack.length>pjax.defaults.maxCacheLength)delete this.mapping[this.backStack.shift()]};Cache.prototype.get=function(id){return this.mapping[id]};Cache.prototype.forward=function(id,value){this.mapping[id]=value;this.backStack.push(id);if(id=this.forwardStack.pop())delete this.mapping[id]};Cache.prototype.back=function(id,value){this.mapping[id]=value;this.forwardStack.push(id);if(id=this.backStack.pop())delete this.mapping[id]};var containerCache=new Cache;pjax.click=handleClick;var popped="state"in window.history,initialURL=location.href;$(window).bind("popstate",function(event){var initialPop=!popped&&location.href==initialURL;popped=true;if(initialPop)return;var state=event.state;if(state&&state.container){var container=$(state.container);if(container.length){var contents=containerCache.get(state.id);if(pjax.state){var direction=pjax.state.id<state.id?"forward":"back";containerCache[direction](pjax.state.id,container.clone(true,true).contents())}var options={id:state.id,url:state.url,container:container,push:false,fragment:state.fragment,timeout:state.timeout,scrollTo:false,duration:state.duration};if(contents){$(document).trigger("pjax",[null,options]);container.trigger("pjax:start",[null,options]);container.trigger("start.pjax",[null,options]);setTimeout(function(){container.html(contents);pjax.state=state;container.trigger("pjax:end",[null,options]);container.trigger("end.pjax",[null,options]);container[0].offsetHeight},state.duration||0)}else{$.pjax(options);container[0].offsetHeight}}else{window.location=location.href}}});if($.inArray("state",$.event.props)<0)$.event.props.push("state");$.support.pjax=window.history&&window.history.pushState&&window.history.replaceState&&!navigator.userAgent.match(/((iPod|iPhone|iPad).+\bOS\s+[1-4]|WebApps\/.+CFNetwork)/);if(!$.support.pjax){$.pjax=function(options){var url=$.isFunction(options.url)?options.url():options.url,method=options.type?options.type.toUpperCase():"GET";var form=$("<form>",{method:method==="GET"?"GET":"POST",action:url,style:"display:none"});if(method!=="GET"&&method!=="POST"){form.append($("<input>",{type:"hidden",name:"_method",value:method.toLowerCase()}))}var data=options.data;if(typeof data==="string"){$.each(data.split("&"),function(index,value){var pair=value.split("=");form.append($("<input>",{type:"hidden",name:pair[0],value:pair[1]}))})}else if(typeof data==="object"){for(key in data)form.append($("<input>",{type:"hidden",name:key,value:data[key]}))}$(document.body).append(form);form.submit()};$.pjax.click=$.noop;$.pjax.reload=window.location.reload;$.fn.pjax=function(){return this}}})(jQuery);